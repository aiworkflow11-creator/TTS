# Original App Setup

This is the original single-file version of the story generator.

## Installation

```
cd /workspaces/TTS/original_app
pip install -r ../requirements.txt
```

## Running

```
python app.py
```

Access at port `5001` (e.g. `http://localhost:5001`).

The UI is very simple and does not split the story.
